# Root convenience env for docker-compose. The API also reads server/.env in dev.
JWT_SECRET=change-me-to-a-long-random-string
WEB_ORIGIN=http://localhost:5173
AI_PROVIDER=mock
EMAIL_PROVIDER=console
SMS_PROVIDER=console
